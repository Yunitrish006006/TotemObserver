## 0.1.3

### Fixed
- Fixed cross-module production map reconstruction data path under Observer runtime tests so zoomed map state is initialized with terrain-backed fixture data instead of a zeroed fallback.
- Added account bridge registration flow to the Observer server runtime.

### Added
- Added account fixture/game test coverage for browser bridge workflows used by the Flutter prototype.
- Added Flutter web client validation step (analyze/test/build) and browser account flow checks in CI.
